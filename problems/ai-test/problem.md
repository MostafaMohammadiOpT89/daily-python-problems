{"slug":"two-sum","problem_md":"Write a Python program that solves the Two Sum problem.\n\nGiven an array of integers `nums` and an integer `target`, return the indices of the two numbers such that they add up to `target`.\n\n### Requirements\n- Implement a function `solve()` that reads input from standard input and writes the result to standard output.\n- The input format is:\n  - First line: an integer `n`, the number of elements in the array\n  - Second line: `n` space-separated integers representing `nums`\n  - Third line: an integer `target`\n- Output the indices of the two numbers as two space-separated integers.\n- If there are multiple valid answers, return any one of them.\n- You may assume exactly one valid answer exists unless otherwise specified.\n\n### Edge cases to consider\n- Duplicate values\n- Negative numbers\n- Zero values\n- The smallest valid array size\n\n### Example\nInput:\n`4\\n2 7 11 15\\n9`\n\nOutput:\n`0 1`","solution_py":"import sys\n\n\ndef two_sum(nums, target):\n    seen = {}\n    for i, num in enumerate(nums):\n        complement = target - num\n        if complement in seen:\n            return [seen[complement], i]\n        seen[num] = i\n    return []\n\n\ndef solve():\n    data = sys.stdin.read().strip().split()\n    if not data:\n        return\n\n    n = int(data[0])\n    nums = list(map(int, data[1:1 + n]))\n    target = int(data[1 + n])\n\n    ans = two_sum(nums, target)\n    if ans:\n        sys.stdout.write(f\"{ans[0]} {ans[1]}\")\n    else:\n        sys.stdout.write(\"\")\n\n\nif __name__ == \"__main__\":\n    solve()\n","test_solution_py":"import io\nimport sys\nimport pytest\n\nfrom solution import two_sum, solve\n\n\ndef run_io(input_data: str) -> str:\n    old_stdin = sys.stdin\n    old_stdout = sys.stdout\n    try:\n        sys.stdin = io.StringIO(input_data)\n        sys.stdout = io.StringIO()\n        solve()\n        return sys.stdout.getvalue()\n    finally:\n        sys.stdin = old_stdin\n        sys.stdout = old_stdout\n\n\ndef test_basic_case():\n    assert two_sum([2, 7, 11, 15], 9) == [0, 1]\n\n\ndef test_duplicate_values():\n    result = two_sum([3, 3], 6)\n    assert result == [0, 1]\n\n\ndef test_negative_numbers():\n    result = two_sum([-3, 4, 3, 90], 0)\n    assert result == [0, 2]\n\n\ndef test_zero_values():\n    result = two_sum([0, 4, 3, 0], 0)\n    assert result == [0, 3]\n\n\ndef test_smallest_valid_array():\n    result = two_sum([1, 2], 3)\n    assert result == [0, 1]\n\n\ndef test_solve_basic_io():\n    assert run_io(\"4\\n2 7 11 15\\n9\\n\") == \"0 1\"\n\n\ndef test_solve_with_duplicates_io():\n    out = run_io(\"2\\n3 3\\n6\\n\")\n    assert out == \"0 1\"\n","explanation_md":"Use a hash map to store numbers we have already seen and their indices.\n\nFor each number `num` at index `i`:\n1. Compute `complement = target - num`.\n2. Check whether `complement` is already in the hash map.\n3. If it is, we have found the answer: return the stored index and `i`.\n4. Otherwise, store `num -> i` in the hash map.\n\nThis works because when we reach the second number of a valid pair, the first number has already been recorded.\n\n### Why this is efficient\n- Each lookup in the hash map is O(1) on average.\n- The array is scanned once.\n- Total time complexity is O(n).\n- Space complexity is O(n) for the hash map.\n\n### Edge cases handled\n- Duplicate numbers like `[3, 3]` with target `6`\n- Negative numbers like `[-3, 4, 3, 90]` with target `0`\n- Zeros like `[0, 4, 3, 0]` with target `0`\n- The smallest valid input size\n\nThe provided tests cover both the core logic and `solve()` input/output behavior using `pytest`."}